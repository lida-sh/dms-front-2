export enum ButtonVariantEnum {
    primary = "primary",
    secondary = "secondary",
    accent = "accent"
}
export enum ToastEnum {
    info = "info",
    warning = "warning",
    error = "error",
    success = "success"
}
export enum ChartType {
    architec = "architec",
    process = "process"

}
